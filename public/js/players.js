// searchPlayers.addEventListener("submit", (e) => {
//     e.preventDefault()
//     var formData = new FormData(e.target);
//     console.log(formData.get("name"))
//     console.log(e.target)
//     var name = formData.get("name").toLowerCase()
//     var filteredList = lineUp.filter(item => {
//         return item.FantasyDraftName.toLowerCase().includes(name)
//     })
//     populatePlayers(filteredList)
// })
// // searchPlayers.addEventListener("formdata", event => {
// //     const data = event.formData;
// //   console.log(data.get("name"))
// //     // get the data
// //     // const entries = [...data.entries()];
// //     // console.log(entries);
  
// //     // const values = [...data.values()];
// //     // console.log(values);
// // });


// // collectPlayers.addEventListener("submit", (e) => {
// //     e.preventDefault()
// // })